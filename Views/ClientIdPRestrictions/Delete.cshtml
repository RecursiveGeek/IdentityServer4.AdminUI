@model IdentityServer4.AdminUI.Models.ClientIdPRestrictions
@using Microsoft.AspNetCore.Http;
@{
    ViewData["Title"] = "Delete";
}
<title>IdPRestrictions - Delete</title>
<hr />
<h3>Are you sure you want to delete this?</h3>
<h2>    @Context.Session.GetString("name")</h2>
<div>
    @* Display for delete information *@
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.ClientId)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.ClientId)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Provider)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Provider)
        </dd>
    </dl>
    <hr />
    @* Post delete and navigation buttons. *@
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="btn btn-info" />
    </form>
    <hr />
    <form asp-action="/Index">
        <input type="submit" value="Back to IdPRestrictions" class="btn btn-info" />
    </form>
    <form asp-controller="Clients" asp-action="Index">

        <input type="submit" value=" Back to Clients" class="btn btn-info" />

    </form>
</div>