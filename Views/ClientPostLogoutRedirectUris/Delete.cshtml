@model IdentityServer4.AdminUI.Models.ClientPostLogoutRedirectUris
@using Microsoft.AspNetCore.Http;
@{
    var ClientName = IdentityServer4.AdminUI.Helpers.VarHelper.ClientName;
}

    <title>Client-PostLogoutRedirectUri-Delete</title>
<hr />

<h3>Are you sure you want to delete this?</h3>
<h2>    @Context.Session.GetString(ClientName)</h2>
@* Display for delete information *@
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.ClientId)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.ClientId)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.PostLogoutRedirectUri)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.PostLogoutRedirectUri)
        </dd>
    </dl>
    <hr />
    <div class="form-check-inline">
        @* Post delete and navigation buttons. *@
        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />
            <input type="submit" value="Delete" class="btn btn-info" />
        </form>
        &nbsp;<i class="fas fa-square-full fa-xs fa-rotate-90"></i>&nbsp;
        <form asp-action="/Index">
            <input type="submit" value="Back to Post Logout Redirect Uri" class="btn btn-info" />
        </form>
        &nbsp;<i class="fas fa-square-full fa-xs fa-rotate-90"></i>&nbsp;
        <form asp-controller="Clients" asp-action="Index">

            <input type="submit" value=" Back to Clients" class="btn btn-info" />

        </form>
    </div>
    </div>
