@model IdentityServer4.AdminUI.Models.ApiScopeClaims
@using Microsoft.AspNetCore.Http;
@{
    var ApiResourceName = IdentityServer4.AdminUI.Helpers.VarHelper.ApiResourceName;
}
    <title>Api-Scope-Claims-Delete</title>

<h2>Delete</h2>
@* Display for delete information *@

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>@Context.Session.GetString(ApiResourceName)</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.ApiScopeId)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.ApiScopeId)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Type)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Type)
        </dd>
    </dl>

    @* Post delete and navigation buttons. *@
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="btn btn-info" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
